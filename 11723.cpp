#include <iostream>
#include <cstring>
using namspace std;

int main() {
	int n;
	scanf("%d", &n);
	while(n--) {
		string input = "";
		cin >> input;
		string cmd = "";
		for(int i=0; i<input.size(); i++)
			if(input[i] != ' ')
				cmd+=inpu[i];


		if( == 'add')
	}
}